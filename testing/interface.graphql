interface Post {
    text: String!
    datePublished: DateTime!
}

type Question implements Post {
    title: String!
    id: Int!
}

type Comment implements Post {
    commentsOn: Post!
    test: Episode!
}

enum Episode {
    NEWHOPE
    EMPIRE
    JEDI
}